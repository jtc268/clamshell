name: macOS

on:
  pull_request:
  push:
    branches: [main]

jobs:
  smoke:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and smoke test
        run: make smoke
